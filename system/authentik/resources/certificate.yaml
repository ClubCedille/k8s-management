apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: authentik-tls
  namespace: authentik
spec:
  secretName: authentik-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  commonName: authentik.mgmt.cedille.club
  dnsNames:
    - authentik.mgmt.cedille.club
  # Optional: Set the duration and renewal time
  duration: 2160h
  renewBefore: 360h
